version: '3.8'

services:
  http-visualizer:
    build:
      context: ..
      dockerfile: http-visualizer-app/Dockerfile
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - RUST_LOG=http_visualizer_app=debug,tower_http=debug
    restart: unless-stopped
